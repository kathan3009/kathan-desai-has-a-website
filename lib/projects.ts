export const projectStatusLabels:Record<string,string>={active:"Active","in-development":"In development",production:"Production",beta:"Beta",prototype:"Prototype",concept:"Concept",archived:"Archived"};
export type PublicProject={id:string;name:string;description:string;techStack:string[];repoUrl:string;liveUrl:string;image:string;type:string;status:string};
export function safeExternalURL(value:unknown):string{if(typeof value!=='string')return '';try{const url=new URL(value);return ['https:','http:'].includes(url.protocol)?url.href:'';}catch{return '';}}
export function projectFromDocument(value:Record<string,unknown>):PublicProject{return {id:String(value._id),name:String(value.name||''),description:String(value.description||''),techStack:Array.isArray(value.techStack)?value.techStack.map(String):[],repoUrl:safeExternalURL(value.repoUrl),liveUrl:safeExternalURL(value.liveUrl),image:typeof value.image==='string'?(value.image.startsWith('/')&&!value.image.startsWith('//')?value.image:safeExternalURL(value.image)):'',type:String(value.type||'other'),status:String(value.status||'active')};}
